# Copy to `.env` in this directory. Loaded by website + desktop Vite and Electron main (dev).

MIRA_API_URL=http://localhost:8000
BETA=

# Optional: desktop renderer dev port (default 59247)
# MIRA_RENDERER_DEV_PORT=59247
